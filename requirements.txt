# speech to text and text to speech
realtimestt==0.3.104
realtimetts[kokoro,coqui,orpheus]==0.5.5

# transformers compatibility (TTS requires BeamSearchScorer from root, available in 4.35.x)
transformers==4.35.2

# webserver dependencies
fastapi
uvicorn

# configuration
python-dotenv

# llm providers
ollama
openai
