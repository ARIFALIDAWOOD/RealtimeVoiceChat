# Development dependencies
# Install with: uv pip install -r requirements-dev.txt

# Pre-commit hooks
pre-commit>=3.5.0

# Code quality tools (already used by pre-commit hooks)
black>=24.1.0
flake8>=7.0.0
isort>=5.13.0
bandit>=1.7.0
detect-secrets>=1.4.0
